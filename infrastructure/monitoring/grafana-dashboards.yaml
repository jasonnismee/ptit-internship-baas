apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-provider
  namespace: monitoring
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: 'default'
        orgId: 1
        folder: ''
        type: file
        disableDeletion: false
        updateIntervalSeconds: 10
        options:
          path: /var/lib/grafana/dashboards
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboards
  namespace: monitoring
data:
  baas.json: |
    {
      "title": "BaaS System Monitor",
      "panels": [
        {
          "type": "timeseries",
          "title": "CPU Activity (Go Routines)",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
          "targets": [ { "expr": "go_goroutines", "refId": "A" } ]
        },
        {
          "type": "timeseries",
          "title": "Memory Allocation (Bytes)",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
          "targets": [ { "expr": "go_memstats_alloc_bytes", "refId": "A" } ]
        }
      ]
    }
